# GitHub Milestone Batch Creator

Creates GitHub milestones using GitHub CLI and TSV files.

## Requirements
- GitHub CLI (`gh`)
- Authenticated (`gh auth login`)

## Usage
1. Set repo in `config.env`
2. Run:
   ```bash
   ./create_milestones.sh
